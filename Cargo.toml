[package]
name = "anomaly_detector"
version = "0.1.0"
edition = "2021"

[dependencies]
pyo3 = { version = "0.21", features = ["extension-module"] }
ndarray = "0.15"

[lib]
name = "anomaly_detector"
crate-type = ["cdylib", "rlib"]
